#!/bin/sh
# Git hook entry point. Delegates to the PowerShell implementation so the logic
# stays in a .ps1 file (required on Windows where pwsh refuses extensionless scripts).
hook_dir=$(dirname "$0")
exec pwsh -NoProfile -ExecutionPolicy Bypass -File "$hook_dir/pre-commit.ps1" "$@"
